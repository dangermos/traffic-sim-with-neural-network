/*fn evolve(
    target: (f32, f32),
    mut parent: (f32, f32),
    copies: usize,
    mutation_rate: u32,
) -> usize {
    let mut counter = 0;
    let mut parent_fitness = fitness();
}

fn fitness() {

}

fn mutate() {
    
}

fn crossover() {
    
} */