[package]
name = "genetics"
version = "0.1.0"
edition = "2024"

[dependencies]
traffic = { path = "../traffic" }
neural = { path = "../neural"}
rayon = "1.11.0"
rand = "0.9.2"
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3.3"
